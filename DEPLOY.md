# 🚀 Инструкция по деплою на Render.com

## Подготовка

1. **Убедитесь, что проект загружен на GitHub**
   ```bash
   git add .
   git commit -m "Prepare for deployment"
   git push origin main
   ```

## Деплой на Render.com

### Шаг 1: Регистрация на Render

1. Перейдите на [render.com](https://render.com)
2. Нажмите **"Get Started"**
3. Зарегистрируйтесь через GitHub

### Шаг 2: Подключение репозитория

1. В Dashboard нажмите **"New +"** → **"Blueprint"**
2. Подключите свой GitHub аккаунт (если еще не подключен)
3. Выберите репозиторий **InvestingApp**
4. Render автоматически обнаружит файл `render.yaml`

### Шаг 3: Настройка переменных окружения

Render автоматически создаст 2 сервиса:
- **investor-portfolio-backend** (Node.js API)
- **investor-portfolio-frontend** (React SPA)

#### Для Backend:
Переменные настроены автоматически в `render.yaml`:
- `NODE_ENV=production`
- `PORT=10000`
- `JWT_SECRET` - генерируется автоматически
- `FRONTEND_URL` - URL фронтенда

#### Для Frontend:
- `VITE_API_URL` - URL бэкенда (настроен в render.yaml)

### Шаг 4: Деплой

1. Нажмите **"Apply"** или **"Create Blueprint"**
2. Render начнет деплой обоих сервисов
3. Подождите ~5-10 минут (первый деплой долгий)

### Шаг 5: Обновление URL

После деплоя Render даст вам реальные URL:
- Backend: `https://investor-portfolio-backend.onrender.com`
- Frontend: `https://investor-portfolio-frontend.onrender.com`

**ВАЖНО:** Обновите переменные окружения с реальными URL:

1. Зайдите в **investor-portfolio-backend** → **Environment**
2. Измените `FRONTEND_URL` на реальный URL фронтенда
3. Зайдите в **investor-portfolio-frontend** → **Environment**
4. Измените `VITE_API_URL` на реальный URL бэкенда + `/api`

### Шаг 6: Первый пользователь

1. Откройте ваш Frontend URL
2. Зарегистрируйте первого пользователя
3. Готово! 🎉

## ⚠️ Важные замечания

### Бесплатный план Render:

- ✅ **Backend** спит после 15 минут неактивности
- ⏱️ Первый запрос после сна займет ~30-60 секунд
- 💾 База данных SQLite хранится локально (при перезапуске НЕ удаляется на Render Web Services)
- 🔄 750 часов в месяц бесплатно

### Альтернативы для базы данных:

Для продакшена рекомендуется использовать внешнюю БД:
- [ElephantSQL](https://www.elephantsql.com/) - бесплатный PostgreSQL
- [PlanetScale](https://planetscale.com/) - бесплатный MySQL
- [MongoDB Atlas](https://www.mongodb.com/atlas) - бесплатный MongoDB

## 🔧 Обновление приложения

При каждом push в `main` ветку GitHub, Render автоматически пересобирает и деплоит приложение!

```bash
git add .
git commit -m "Update app"
git push origin main
# Render автоматически задеплоит изменения
```

## 📊 Мониторинг

- **Логи**: Dashboard → Ваш сервис → Logs
- **Метрики**: Dashboard → Ваш сервис → Metrics
- **Health Check**: `https://your-backend.onrender.com/api/health`

## 🆘 Troubleshooting

### Backend не запускается:
1. Проверьте логи в Dashboard
2. Убедитесь, что все зависимости в `package.json`
3. Проверьте переменные окружения

### Frontend показывает ошибки API:
1. Проверьте `VITE_API_URL` в настройках Frontend
2. Проверьте CORS настройки в Backend
3. Дождитесь "пробуждения" Backend (первый запрос после сна)

### База данных пустая:
1. Backend автоматически инициализирует БД при первом запуске
2. Проверьте логи: должно быть "База данных инициализирована"

## 🎯 Готово!

Ваше приложение теперь доступно онлайн 24/7! 🚀

