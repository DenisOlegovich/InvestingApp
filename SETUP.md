# Инструкция по запуску приложения

## 📋 Требования
- Node.js версии 16 или выше
- npm или yarn

## 🚀 Быстрый старт

### 1. Запуск Backend

```bash
# Перейти в папку backend
cd backend

# Установить зависимости (если еще не установлены)
npm install

# Инициализировать базу данных (только первый раз)
npm run init-db

# Запустить сервер
npm start
```

Бэкенд будет доступен на `http://localhost:3001`

### 2. Запуск Frontend

```bash
# В новом терминале, из корня проекта
npm install  # если еще не установлены зависимости
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

## 👤 Первый вход

1. Откройте `http://localhost:5173` в браузере
2. Нажмите "Зарегистрироваться"
3. Заполните форму:
   - Имя: ваше имя
   - Email: ваш email
   - Пароль: минимум 6 символов
4. После регистрации вы автоматически войдете в систему

## 🔧 Возможные проблемы

### Порт 3001 занят
Если бэкенд не запускается на порту 3001:
```bash
# Проверить, что занимает порт
lsof -ti:3001

# Остановить процесс
kill -9 $(lsof -ti:3001)
```

### CORS ошибки
Убедитесь, что frontend запущен на `http://localhost:5173` (порт по умолчанию для Vite)

### Ошибка подключения к API
1. Проверьте, что бэкенд запущен (`http://localhost:3001` должен отвечать)
2. Проверьте файл `.env` в корне проекта - должен содержать:
   ```
   VITE_API_URL=http://localhost:3001/api
   ```
3. Перезапустите frontend после изменения .env

## 📊 Структура данных

После авторизации все данные сохраняются в SQLite базу данных (`backend/database.sqlite`).

Каждый пользователь имеет свой изолированный портфель с:
- Ценными бумагами
- Недвижимостью
- Депозитами
- Криптовалютами

## 🔐 Безопасность

- Пароли хешируются с bcrypt
- JWT токены действительны 7 дней
- Автоматический выход при истечении токена
- Защита от SQL injection

## 🧪 Тестовые данные

Для быстрого тестирования используйте:
- Email: `test@test.com`
- Пароль: `123456`

(Уже создан при первом тестовом запуске)

## 📝 Дополнительно

Просмотр логов бэкенда:
```bash
cd backend
tail -f backend.log
```

Очистка базы данных (все данные будут удалены!):
```bash
cd backend
rm database.sqlite
npm run init-db
```

