# 🚀 Инструкция по публикации на бесплатном хостинге

## Обзор
Мы используем:
- **Frontend**: [Vercel](https://vercel.com) - бесплатный хостинг для React приложений
- **Backend**: [Render](https://render.com) - бесплатный хостинг для Node.js API

---

## 📦 Шаг 1: Подготовка GitHub репозитория

1. **Убедитесь, что проект загружен на GitHub**:
```bash
git status
git add .
git commit -m "Подготовка к деплою"
git push origin main
```

---

## 🌐 Шаг 2: Деплой Backend на Render

### 2.1 Создание аккаунта
1. Зайдите на [render.com](https://render.com)
2. Зарегистрируйтесь через GitHub

### 2.2 Создание Web Service
1. Нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий
3. Настройте параметры:
   - **Name**: `investor-portfolio-api`
   - **Region**: `Frankfurt (EU Central)`
   - **Branch**: `main`
   - **Root Directory**: `backend`
   - **Runtime**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: **Free**

### 2.3 Переменные окружения
В разделе **Environment Variables** добавьте:

```
NODE_ENV=production
JWT_SECRET=<генерируйте случайную строку, например: a8f7d9e2c1b3f4g5h6j7k8l9>
FRONTEND_URL=https://your-app.vercel.app
```

⚠️ **ВАЖНО**: Замените `your-app.vercel.app` на реальный URL вашего фронтенда после деплоя на Vercel (Шаг 3)

### 2.4 Деплой
1. Нажмите **"Create Web Service"**
2. Дождитесь завершения сборки (~5 минут)
3. Скопируйте URL вашего API (например: `https://investor-portfolio-api.onrender.com`)

---

## 💻 Шаг 3: Деплой Frontend на Vercel

### 3.1 Создание аккаунта
1. Зайдите на [vercel.com](https://vercel.com)
2. Зарегистрируйтесь через GitHub

### 3.2 Import проекта
1. Нажмите **"Add New..."** → **"Project"**
2. Выберите ваш GitHub репозиторий
3. Настройте параметры:
   - **Framework Preset**: Vite
   - **Root Directory**: `./` (корень проекта)
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`

### 3.3 Переменные окружения
В разделе **Environment Variables** добавьте:

```
VITE_API_URL=https://investor-portfolio-api.onrender.com/api
```

⚠️ **ВАЖНО**: Замените URL на реальный адрес вашего бэкенда из Шага 2.4

### 3.4 Деплой
1. Нажмите **"Deploy"**
2. Дождитесь завершения (~2 минуты)
3. Скопируйте URL вашего приложения (например: `https://your-app.vercel.app`)

---

## 🔄 Шаг 4: Обновление CORS в Backend

После получения URL фронтенда:

1. Вернитесь в **Render** → ваш Web Service → **Environment**
2. Обновите переменную `FRONTEND_URL`:
```
FRONTEND_URL=https://your-app.vercel.app
```
3. Нажмите **"Save Changes"**
4. Backend автоматически перезапустится

---

## ✅ Готово!

Ваше приложение опубликовано! 🎉

- **Frontend**: `https://your-app.vercel.app`
- **Backend API**: `https://investor-portfolio-api.onrender.com`

---

## 🔧 Автоматическое обновление

После настройки:
- При каждом `git push` в ветку `main`:
  - Vercel автоматически обновит фронтенд
  - Render автоматически обновит бэкенд

---

## ⚠️ Ограничения бесплатного хостинга

### Render (Backend)
- ❄️ **Засыпание**: сервис засыпает после 15 минут неактивности
- ⏰ **Пробуждение**: первый запрос после сна занимает ~30 секунд
- 💾 **База данных**: SQLite хранится на диске (может быть сброшена при перезапуске)
- ⏱️ **Часы работы**: 750 часов/месяц бесплатно

### Vercel (Frontend)
- ✅ **Всегда активен**: нет засыпания
- 📊 **Трафик**: 100 GB/месяц
- 🚀 **Билды**: 100 билдов/месяц

---

## 🐛 Решение проблем

### Backend не отвечает
1. Проверьте логи в Render Dashboard
2. Убедитесь, что переменные окружения установлены
3. Проверьте, что сервис не заснул (откройте URL в браузере)

### Frontend не подключается к Backend
1. Проверьте переменную `VITE_API_URL` в Vercel
2. Проверьте CORS настройки в Backend (`FRONTEND_URL`)
3. Откройте DevTools (F12) → Console для ошибок

### База данных сброшена
На бесплатном tier Render может сбросить файловую систему. Для продакшена рекомендуется использовать PostgreSQL.

---

## 📝 Дополнительные команды

### Локальная разработка
```bash
# Frontend
npm run dev

# Backend
cd backend
npm run dev
```

### Пересборка
```bash
# Frontend
npm run build

# Backend (не требуется)
```

---

## 🔐 Безопасность

⚠️ **Никогда не коммитьте в Git**:
- `.env` файлы
- `database.sqlite`
- JWT секреты

Все секреты должны быть только в Environment Variables на хостинге!

---

Удачи с вашим портфолио! 📊💰

